<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.oBootMybatis01.EmpMapper">
	
    <!-- 1. totalCount 조회-->
	<select id="empTotal" resultType="int">
		select Count(*) FROM emp
	</select>


	<!-- 2. List 조회 -> emp 전체 Select -->
	<select id="tkEmpListAll" parameterType="Emp" resultType="Emp">
		SELECT *
		FROM
		(
			SELECT rownum rn, a.*
			FROM
					(SELECT * FROM emp ORDER BY empno) a
		)
		WHERE rn BETWEEN #{start} AND #{end}	
	</select>
	
	<!-- 3. 직원 정보 -->
	<select id="tkEmpOne" parameterType="int" resultType="Emp">
		SELECT * FROM EMP WHERE empno= #{empno}
	</select>
	
	<!-- 5. 수정 후 업데이트 -->
	<update id="tkEmpUpdate" parameterType="Emp">
		Update Emp 
		Set ename=#{ename}, job=#{job}, sal=#{sal}, hiredate=#{hiredate}, 
			<if test="#{comm} != null">comm=#{comm}, </if>  
			mgr=#{mgr}, deptno=#{deptno} 
		WHERE empno=#{empno}
	</update>
	
	<!-- 6-1. 관리자 사번만 가져오는 것 -->
	<select id="tkSelectManager" resultType="Emp">
		SELECT *
		FROM emp
		WHERE empno in (select mgr from emp)
	</select>
	
	<!-- 입력 -->
	<insert id="insertEmp" parameterType="Emp">
		INSERT INTO EMP VALUES (#{empno}, #{ename}, #{job}, #{mgr}, #{hiredate}, #{sal}, #{comm}, #{deptno}) 
	</insert>
	
	<!-- 삭제 -->
	<delete id="ijDeleteEmp" parameterType="int">
		Delete FROM EMP WHERE empno = #{empno}
	</delete>
	
	<!-- CDATA 사용방법 -->
	<select id="tkEmpListAll2" parameterType="Emp" resultType="Emp">
		<![CDATA[
			SELECT *
			FROM
			(
				SELECT rownum rn, a.*
				FROM
						(SELECT * FROM emp ORDER BY empno) a
			)
			WHERE 	rn >= #{start} 
			AND 	rn <= #{end}
		]]>	
	</select>	

	<!-- 10-2. 검색 -->
   <select id="tkEmpSearchList3" parameterType="Emp"  resultType="Emp" >
		SELECT *
		FROM
		(
		        SELECT rownum rn , a.*
		        FROM
		                   (select * from emp 
		                   	WHERE
		                   <choose>
					 			<when test="search == 's_job'">
					                 job  like '%' || #{keyword} || '%'    
					  			</when>
					  			<when test="search == 's_ename'">
					                 ename like '%' || #{keyword} || '%'    
					  			</when>
					            <otherwise>
					                 job   like '%'   
					            </otherwise>
        					</choose>
        					order by empno
		                   ) a
		 )   
 		WHERE  rn BETWEEN #{start} and #{end} 
 		
     
    </select>
    
    
    <!-- 10-1. 검색조건에 따른 Count 조회 -->
	<select id="condEmpTotal" parameterType="Emp" resultType="int">
		select Count(*) FROM emp 
		<choose>
			<when test="search == 's_job'">
				WHERE job like '%' || #{keyword} || '%'
			</when>
			<when test="search == 's_ename'">
				WHERE ename like '%' || #{keyword} || '%'
			</when>
			<otherwise>
				WHERE job like '%'
			</otherwise>
		</choose>
	</select>
	
</mapper>